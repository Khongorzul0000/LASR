const originalArray = [1, 2, 3, 4, 5];

// originalArray.push(100);
// originalArray.unshift(100);
// originalArray.pop(100);
// originalArray.shift(100);

// const fake = originalArray.slice(2);
// const real = originalArray.filter((value) => !== 3)
// const just = originalArray.splice(4, 1)
// const just = originalArray.splice(startIndex, takeIndex, newItem, newItem)

// 4 index 1 ni hediig avah 
const lastIndex = originalArray.length - 1


// console.log("original arr:", originalArray)
// console.log("new:", just)
console.log("last;", lastIndex)